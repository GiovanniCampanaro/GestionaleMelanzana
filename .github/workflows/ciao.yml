name: ciao
run-name: ciao da parte di ${{ github.actor }}
on: [push]
jobs:
  ciao:
    runs-on: ubuntu-latest
    steps:
      - run: mvn -v
      - uses: actions/checkout@v4
      - run: |
          ls -la
          cd javademo/my-app
          mvn install
      - run: echo "lesgoski"
  verifica-cartella:
    runs-on: ubuntu-latest
    steps:
      - run: mvn -v
      - uses: actions/checkout@v4
      - run: |
          ls -la
      - run: echo "verifica cartella"